#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "managewrapping.h"

/*

Legger alle verdiene som trengs i pakken inn en struct. Returnerer peker til structen.

*/
struct packet* wrap(char* buf){

	struct packet *p;
	//p->length = strlen(buf);
	//p->data = buf;

	return p;
}

void unwrap(){


}